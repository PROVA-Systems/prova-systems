# PROVA ENV-Audit — 2026-05-07

**Anlass:** AWS-Lambda-4KB-ENV-Limit Deploy-Block am 2026-05-07 15:12:55
**Sprint:** MEGA¹⁵.5 W37
**Audit-Methode:** `grep -rh "process\.env\." netlify/functions/`
**Total ENVs im Code referenziert:** 85+ (siehe vollstaendige Liste unten)

---

## 1. Klassifikation

### 1.1 AWS-Built-In (KEINE Reduktion noetig — werden von Lambda automatisch bereitgestellt, zaehlen ggf. nicht zum 4KB-Limit)

```
AWS_LAMBDA_FUNCTION_MEMORY_SIZE
AWS_REGION
COMMIT_REF
CONTEXT
DEPLOY_PRIME_URL
DEPLOY_URL
SITE_NAME
URL
```
**8 Vars — read-only, automatisch.**

---

### 1.2 PFLICHT-PRODUCTION (alle Functions)

Diese muessen in JEDER Function verfuegbar sein:

| ENV | Zweck | Pflicht |
|---|---|---|
| `SUPABASE_URL` | Supabase-Endpoint | ja (alle DB-Functions) |
| `SUPABASE_SERVICE_ROLE_KEY` | Server-side Supabase-Auth | ja (Server-side-Pflicht) |
| `OPENAI_API_KEY` | KI-Calls | ja (ki-proxy + Verwandte) |
| `ANTHROPIC_API_KEY` | KI-Fallback | ja (ki-proxy) |
| `AUTH_HMAC_SECRET` | Auth-Token-Signing | ja (auth-token-issue) |
| `PROVA_INTERNAL_WRITE_SECRET` | Internal-Auth | ja (alle write-functions) |
| `PROVA_SUPABASE_JWKS_URL` | JWT-Validation | ja (alle auth-functions) |
| `PROVA_SUPABASE_PROJECT_URL` | Supabase-Project | ja |

**8 Vars — koennen NICHT geloescht werden.**

---

### 1.3 KONSOLIDIERBAR (sofortige Reduktion durch Code-Refactor)

#### A) MAKE_WEBHOOKS (21 Vars → 1 JSON)
**Aktuelle Vars:**
```
MAKE_WEBHOOK_A5, _F1, _G1, _G3, _K1, _K2, _K3, _KAUF, _L3-L5,
_L8-L10, _S1, _S3, _S6, _S9, _SUPPORT, _TRIAL, _WHISPER, _WILLKOMMEN
+ MAKE_S3_WEBHOOK, MAKE_S4_WEBHOOK
```

**Loesung:** Single ENV `MAKE_WEBHOOKS` (JSON-Object):
```bash
MAKE_WEBHOOKS='{"a5":"https://...","f1":"https://...","g1":"...",...}'
```

**Helper:** `lib/make-webhooks.js` mit `getMakeWebhook(key)` + Backwards-Compat.

**Reduktion:** 21 Vars → 1 Var = **-20 Vars**

#### B) STRIPE_PRICES (5 Vars → 1 JSON oder Defaults entfernen)
**Aktuelle Vars (alle OPTIONAL — Defaults im Code):**
```
STRIPE_PRICE_SOLO, _TEAM, _ADDON_5, _ADDON_10, _ADDON_20
```

**Existing Helper:** `netlify/functions/lib/prova-stripe-prices.js` hat bereits Defaults — diese 5 Vars sind nur OVERRIDE.

**Loesung 1 (einfach):** Marcel **loescht** die 5 ENV-Vars aus Netlify — Code nutzt Defaults.
**Loesung 2 (flexibel):** `STRIPE_PRICES` JSON wenn Override-Bedarf besteht.

**Reduktion (Variante 1):** **-5 Vars**

#### C) AIRTABLE-Auth (3 redundante Vars → 1)
**Aktuelle Vars (alle gleicher Zweck — historische Migration):**
```
AIRTABLE_PAT      ← aktuell verwendet (modernster Name)
AIRTABLE_TOKEN    ← veraltet (Sprint K-1 ENV-Cleanup vergessen?)
AIRTABLE_API_KEY  ← veraltet (alter Naming-Style)
```

**Loesung:** Marcel **loescht** AIRTABLE_TOKEN und AIRTABLE_API_KEY.

**Code-Stellen** (mit Default-Resolution):
- `airtable.js`: nutzt AIRTABLE_PAT
- `health.js`: hat Fallback `AIRTABLE_PAT || AIRTABLE_TOKEN` — Fallback bleibt safety-net

**Reduktion:** **-2 Vars**

#### D) STRIPE_PRICE_ (leerer Suffix)
Im Code: `process.env.STRIPE_PRICE_` (eindeutig Tippfehler oder vergessenes Snippet)
**Loesung:** Code-Stelle finden + bereinigen.

**Reduktion:** **-1 Var** (wenn diese Variable aktuell in Netlify gesetzt ist)

---

### 1.4 FUNCTION-SCOPE-FAEHIG (kein Code-Change — Marcel-Pflicht im Netlify-UI)

Diese ENVs werden NUR in spezifischen Functions gebraucht. Function-Scope reduziert die-pro-Function-ENV-Last:

| ENV-Group | Pattern | Functions die brauchen |
|---|---|---|
| **STRIPE_*** (alle) | stripe-* | stripe-webhook, stripe-checkout, stripe-* (5 Functions) |
| **ADMIN_*** | admin-* | admin-dashboard, admin-system-health, admin-* (~22 Functions) |
| **AIRTABLE_*** | airtable* | airtable.js, airtable-write, etc. (~8 Functions) |
| **MAKE_WEBHOOKS** | webhook-trigger | emails.js, make-proxy.js, team-interest.js (3 Functions) |
| **VAPID_*** | push-notification | (~2 Functions) |
| **IONOS_SMTP_*** | email | emails.js, support-* (~3 Functions) |
| **PROVA_SMTP_*** | email | emails.js, eingeladen-* (~5 Functions) |
| **SENTRY_*** | alle | optional in allen Functions (Function-Scope nicht zwingend) |
| **PDFMONKEY_*** | pdf-generate | pdf-* Functions (~3) |

**Marcel-Action:** pro ENV im Netlify-UI "Specific scopes" waehlen (siehe `marcel-actions-required.md`).

---

### 1.5 VERALTET / DEV-ONLY (Loesch-Kandidaten)

**Marcel-OK pflicht vor Loeschung!**

| ENV | Begruendung | Risk |
|---|---|---|
| `AIRTABLE_TOKEN` | redundant zu AIRTABLE_PAT | low (Fallback in health.js) |
| `AIRTABLE_API_KEY` | redundant zu AIRTABLE_PAT | low |
| `STRIPE_PRICE_` (leer) | leerer Suffix = Typo | none |
| `MAKE_S3_WEBHOOK` | duplikat zu MAKE_WEBHOOK_S3? | medium — Code-Audit pflicht |
| `MAKE_S4_WEBHOOK` | duplikat zu MAKE_WEBHOOK_S4? | medium |
| `NETLIFY_IDENTITY_REDIRECT_URL` | hybrid-modus alt | hoch wenn noch Login-Flow nutzt |
| `NETLIFY_EMAILS_DIRECTORY` | selten genutzt | low |
| `STRIPE_PRICE_SOLO/TEAM/ADDON_*` | Defaults in Code | low (Code-Default greift) |

---

## 2. Vollstaendige Liste — alle 85 ENVs

```
ADMIN_PASSWORD_BCRYPT       (admin-auth)
ADMIN_PASSWORD_HASH         (admin-auth)
AIRTABLE_API_KEY            (LOESCH-KANDIDAT)
AIRTABLE_AUDIT_TRAIL_TABLE  (audit)
AIRTABLE_BASE_ID            (alle Airtable)
AIRTABLE_BRIEFE_TABLE       (briefe)
AIRTABLE_PAT                (PFLICHT — modernster Auth-Name)
AIRTABLE_TABLE_SV           (sv-records)
AIRTABLE_TOKEN              (LOESCH-KANDIDAT)
ANTHROPIC_API_KEY           (PFLICHT — KI-Fallback)
AUTH_HMAC_SECRET            (PFLICHT)
AWS_LAMBDA_FUNCTION_MEMORY_SIZE  (BUILT-IN)
AWS_REGION                  (BUILT-IN)
COMMIT_REF                  (BUILT-IN)
CONTEXT                     (BUILT-IN)
DEPLOY_PRIME_URL            (BUILT-IN)
DEPLOY_URL                  (BUILT-IN)
IONOS_SMTP_HOST             (FUNCTION-SCOPE)
IONOS_SMTP_PASS             (FUNCTION-SCOPE)
IONOS_SMTP_USER             (FUNCTION-SCOPE)
MAKE_S3_WEBHOOK             (LOESCH-KANDIDAT — duplikat?)
MAKE_S4_WEBHOOK             (LOESCH-KANDIDAT — duplikat?)
MAKE_WEBHOOK_A5             (KONSOLIDIEREN → MAKE_WEBHOOKS)
MAKE_WEBHOOK_F1             (KONSOLIDIEREN)
MAKE_WEBHOOK_G1             (KONSOLIDIEREN)
MAKE_WEBHOOK_G3             (KONSOLIDIEREN)
MAKE_WEBHOOK_K1             (KONSOLIDIEREN)
MAKE_WEBHOOK_K2             (KONSOLIDIEREN)
MAKE_WEBHOOK_K3             (KONSOLIDIEREN)
MAKE_WEBHOOK_KAUF           (KONSOLIDIEREN)
MAKE_WEBHOOK_L10            (KONSOLIDIEREN)
MAKE_WEBHOOK_L3             (KONSOLIDIEREN)
MAKE_WEBHOOK_L4             (KONSOLIDIEREN)
MAKE_WEBHOOK_L5             (KONSOLIDIEREN)
MAKE_WEBHOOK_L8             (KONSOLIDIEREN)
MAKE_WEBHOOK_L9             (KONSOLIDIEREN)
MAKE_WEBHOOK_S1             (KONSOLIDIEREN)
MAKE_WEBHOOK_S3             (KONSOLIDIEREN)
MAKE_WEBHOOK_S6             (KONSOLIDIEREN)
MAKE_WEBHOOK_S9             (KONSOLIDIEREN)
MAKE_WEBHOOK_SUPPORT        (KONSOLIDIEREN)
MAKE_WEBHOOK_TRIAL          (KONSOLIDIEREN)
MAKE_WEBHOOK_WHISPER        (KONSOLIDIEREN)
MAKE_WEBHOOK_WILLKOMMEN     (KONSOLIDIEREN)
OPENAI_API_KEY              (PFLICHT)
PDFMONKEY_API_KEY           (FUNCTION-SCOPE pdf-*)
PDFMONKEY_FOTO_TEMPLATE_ID  (FUNCTION-SCOPE pdf-*)
PDF_PROXY_SECRET            (FUNCTION-SCOPE pdf-*)
PROVA_ADMIN_REQUIRE_2FA     (admin-*)
PROVA_AUDIT_TRAIL_TABLE     (alle write)
PROVA_INTERNAL_SECRET       (admin)
PROVA_INTERNAL_WRITE_SECRET (PFLICHT)
PROVA_MIGRATION_PATH        (PFLICHT alle DB)
PROVA_SENTRY_TEST_SECRET    (LOESCH-KANDIDAT — test-only)
PROVA_SMTP_ENCRYPTION_KEY   (FUNCTION-SCOPE email-*)
PROVA_SMTP_FROM_NAME        (FUNCTION-SCOPE)
PROVA_SMTP_HOST             (FUNCTION-SCOPE)
PROVA_SMTP_PASS             (FUNCTION-SCOPE)
PROVA_SMTP_PORT             (FUNCTION-SCOPE)
PROVA_SMTP_USER             (FUNCTION-SCOPE)
PROVA_SUPABASE_JWKS_URL     (PFLICHT)
PROVA_SUPABASE_PROJECT_URL  (PFLICHT)
SENTRY_AUTH_TOKEN           (FUNCTION-SCOPE)
SENTRY_DSN_FUNCTIONS        (FUNCTION-SCOPE)
SENTRY_ORG_SLUG             (FUNCTION-SCOPE)
SENTRY_PROJECT_SLUG_BROWSER (FUNCTION-SCOPE)
SENTRY_PROJECT_SLUG_FUNCTIONS (FUNCTION-SCOPE)
SITE_NAME                   (BUILT-IN)
STRIPE_AUTO_TAX             (FUNCTION-SCOPE stripe-*)
STRIPE_FOUNDING_COUPON_ID   (FUNCTION-SCOPE stripe-*)
STRIPE_PRICE_               (LOESCH — leerer Typo)
STRIPE_PRICE_ADDON_10       (LOESCH — Default in Code)
STRIPE_PRICE_ADDON_20       (LOESCH — Default in Code)
STRIPE_PRICE_ADDON_5        (LOESCH — Default in Code)
STRIPE_PRICE_SOLO           (LOESCH — Default in Code)
STRIPE_PRICE_TEAM           (LOESCH — Default in Code)
STRIPE_SECRET_KEY           (FUNCTION-SCOPE stripe-*)
STRIPE_WEBHOOK_SECRET       (FUNCTION-SCOPE stripe-webhook)
SUPABASE_SERVICE_ROLE_KEY   (PFLICHT alle)
SUPABASE_URL                (PFLICHT alle)
TEAM_INTEREST_RATE_LIMIT_IP_PER_MIN (FUNCTION-SCOPE team-interest)
TEAM_INTEREST_SECRET        (FUNCTION-SCOPE)
TERMIN_REMINDER_SECRET      (FUNCTION-SCOPE termin-*)
UPTIME_WEBHOOK_SECRET       (FUNCTION-SCOPE uptime-webhook)
URL                         (BUILT-IN)
VAPID_PRIVATE_KEY           (FUNCTION-SCOPE push-*)
VAPID_PUBLIC_KEY            (FUNCTION-SCOPE push-*)
VAPID_SUBJECT               (FUNCTION-SCOPE push-*)
```

---

## 3. Reduktions-Strategie Summary

| Aktion | Gewinn |
|---|---:|
| MAKE_WEBHOOK_* → 1 JSON | **-20 Vars** |
| STRIPE_PRICE_* loeschen (Defaults im Code) | **-5 Vars** |
| AIRTABLE_TOKEN + _API_KEY loeschen | **-2 Vars** |
| MAKE_S3/S4_WEBHOOK loeschen wenn duplikat | **-2 Vars** |
| STRIPE_PRICE_ (leer) loeschen | **-1 Var** |
| PROVA_SENTRY_TEST_SECRET loeschen | **-1 Var** |
| **Total Konsolidierung** | **-31 Vars** |

**85 - 31 = 54 Vars** nach Konsolidierung.

**Plus Function-Scopes:** pro Function nur ~10-15 ENVs verfuegbar = unter 4KB-Limit.

---

## 4. Risiken bei Loeschung (vor jeder Loeschung pruefen!)

- **AIRTABLE_TOKEN/API_KEY**: health.js hat Fallback `pat || token` — sicher wenn pat gesetzt
- **STRIPE_PRICE_***: Code hat Defaults — sicher wenn Marcel KEINE Stripe-Preis-Aenderung in Netlify-UI gemacht hat
- **MAKE_S3/S4_WEBHOOK**: Code-Audit pflicht (gibt es noch Aufrufe in Make-Scenarios?)

---

## 5. Marcel-Reihenfolge (priorisiert)

1. **SOFORT-Loeschungen** (sicher, kein Code-Refactor): -8 Vars
   - AIRTABLE_TOKEN, AIRTABLE_API_KEY
   - 5 STRIPE_PRICE_* (Defaults im Code)
   - STRIPE_PRICE_ (leerer Typo)
   - PROVA_SENTRY_TEST_SECRET

2. **Konsolidierung MAKE_WEBHOOKS** (Code-Helper W38): -20 Vars

3. **Function-Scopes setzen** (Marcel-UI-Action W40)

4. **Deploy-Test:** wenn FAIL, weiter konsolidieren

---

*Audit-Stand 2026-05-07. Naechster Schritt: lib/make-webhooks.js Helper.*
